# Local discussion/session docs (not part of codebase)
CLAUDE.md
report.md

# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# ANTLR4 generates files in a grammar/ subdir when run inside Docker;
# the correct copies are at src/generated/*.py (not src/generated/grammar/)
src/generated/grammar/
